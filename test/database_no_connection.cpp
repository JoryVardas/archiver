#include <catch2/catch_all.hpp>

TEST_CASE("Connecting to the database", "[database]") {
  WARN("No database parameters were specified when the tests were built, as "
       "such database tests could not be built!");
}